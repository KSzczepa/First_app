export interface TaskData {
  id: string;
  userId: string;
  title: string;
  summary: string;
  dueDate: string;
}

export interface FormData {
    title: string;
    summary: string;
    dueDate: string;
}